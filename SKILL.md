---
name: ppt-generator
description: 将文案填入北京理工大学 BIT 模板，生成一页指定版式的 PPT。
disable-model-invocation: false
user-invocable: true
---

# ppt-generator

把各种文案填入 PPT 模板，每次生成**一页**标题页/结尾页/正文页。目标模板为北京理工大学
答辩/汇报模板（BIT）。

## 何时使用

当任务需要"生成 PPT 页面"（如标题页、结尾页、要点页）时使用。
根据不同文案类型可选择不同的版式进行生成。
若文案包含标题以及个人信息，可选择标题页或结尾页。
若文案包含一到四段文本，且每段都可拆出小标题与正文时可选择正文页。
一次调用对应一页；若需多页需多次调用（本技能不支持一次拼多页）。

## 按文案类型选择版式

| 文案类型 | 子命令     | 必填参数                          |
|------|---------|-------------------------------|
| 标题页  | `title` | `--title --name --teacher`    |
| 结尾页  | `end`   | `--title --name --teacher`    |
| 一段文本 | `1text` | `--title --subtitles --texts` |
| 两段文本 | `2text` | `--title --subtitles --texts` |
| 三段文本 | `3text` | `--title --subtitles --texts` |
| 四段文本 | `4text` | `--title --subtitles --texts` |

参数含义：
- `--title`：若选择标题页或结尾页，则为页面中央的大标题；若选择正文页，则为页面最上方的页面标题，表明整页主题。
- `--name`：答辩人姓名。
- `--teacher`：导师姓名。
- `--subtitles`：所有小标题，不同段落的小标题必须各自作为一个独立参数传递，按顺序输入，**不宜过长**。
- `--texts`：所有正文，不同段落的正文必须各自作为一个独立参数传递，按顺序输入。

可选参数：
- `-o` 或 `--output`：用于指定输出文件路径。

## 正文长度上限

每段正文不得超过下列上限（超出易溢出版面），也不应字数过少（过少会使版面过于空旷）：

| 子命令     | 每段正文上限 |
|---------|--------|
| `1text` | 200 字  |
| `2text` | 100 字  |
| `3text` | 60 字   |
| `4text` | 80 字   |

## 命令结构

入口为技能内的 `scripts/main.py`。命令需在技能根目录下执行。要点：
- **`-o` 或 `--output` 必须写在子命令之前**。
- 未指定 `-o` 时输出到默认位置，**建议总是显式给出输出路径与文件名**。
- 注意区分 `title` 子命令（用于选择生成标题页）以及 `--title` 参数（用于输入页面标题字符串）。

```
python scripts/main.py -o "<输出路径>.pptx" <子命令> <参数...>
```

## 执行步骤

1. 依任务确定文案内容及其类型，据此选择对应的子命令。
2. 由于任务涉及写文件操作，向用户确认输出路径及调用操作。若用户同意，继续执行以下步骤；若否定，则停止调用。
3. 根据选择的子命令组装各个参数。
4. 以"运行示例"中的结构调用脚本，`-o` 放在子命令之前并给出输出文件路径。
5. 依据脚本输出确认成功。

## 强制前置确认（不可跳过）

在执行脚本前必须向用户进行二次确认，详细列出即将执行的命令与预计结果。

**在用户明确回复确认之前，禁止执行 `scripts/main.py`。**

**以下情况一律不算已确认：**
- 用户只是给定输出路径
- 用户只是授予了文件/命令权限
- 用户要求直接生成，不需要确认
- 用户确认了本次命令的同时提出了第二次调用，则第二次仍需确认

## 运行示例

标题页：
```bash
python scripts/main.py -o page.pptx title --title "人工智能系统答辩" --name "张三" --teacher "李四"
```

两段文本：
```bash
python scripts/main.py -o page.pptx 2text --title "研究背景与意义" --subtitles "研究背景" "研究意义" --texts "深度学习在图像识别等任务上已取得重大突破。" "自动化工具可显著提升文档生产与演示效率。"
```

三段文本：
```bash
python scripts/main.py -o arch.pptx 3text --title "系统架构设计" --subtitles "输入层" "处理层" "输出层" --texts "负责数据清洗与特征提取。" "执行核心算法与业务逻辑。" "汇总结果并生成可视化报告。"
```
